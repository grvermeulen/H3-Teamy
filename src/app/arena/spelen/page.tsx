import { ArenaModePage } from "@/components/cityArena/ArenaModePage";

/** Entry for own-screen, controller and mirrored hybrid modes. */
export default function ArenaPlayPage(): React.JSX.Element {
  return <ArenaModePage defaultRole="player" />;
}
